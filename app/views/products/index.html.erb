<h1>Welcome to Our Fun Store</h1>

<p>
  <%= link_to 'Add New Product', new_product_path %>
</p>

<ul>

<% @products.each do |product| %>
  <li>
    <p><%= link_to product.name,  product_path(product.id)%></p>
    <%= render 'product_details', product: product %>
  </li>
  <% end %>

</ul>
